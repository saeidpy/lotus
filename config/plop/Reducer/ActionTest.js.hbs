import * as actions from './{{name}}Action'


describe('actions', () => {
  it('should create an action to add a todo', () => {
    const payload = 'Finish docs'
    const expectedAction = {
      type: actions.{{type}},
      payload
    }
    expect(actions.{{nameFunc}}(payload)).toEqual(expectedAction)
  })
})